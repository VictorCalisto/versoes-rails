version: '3'

services:

  ruby:
    container_name: ruby1
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    volumes:
      - .:/app
      - gems:/usr/local/bundle
   
volumes:
  gems:
