version: '3'

services:

  ruby:
    container_name: ruby2
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3002:3000"
    volumes:
      - .:/app
      - gems:/usr/local/bundle

volumes:
  gems:
