version: '3'

services:

  ruby:
    container_name: ruby3
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3003:3000"
    volumes:
      - .:/app
      - gems:/usr/local/bundle
   
volumes:
  gems:
